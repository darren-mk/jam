(ns mongerupclj.core)

;; 학습 네임스페이스 목록
;;
;; mongerupclj.db               → 공통 DB 연결
;; mongerupclj.crud             → INSERT / FIND / UPDATE / DELETE / COUNT
;; mongerupclj.query-operators  → $gt $lt $in $or $exists $regex 등
;; mongerupclj.update-operators → $set $inc $push $pull $unset 등       (예정)
;; mongerupclj.nested           → 내장 문서 & 배열 쿼리                  (예정)
;; mongerupclj.indexes          → 인덱스 & 성능                          (예정)
;; mongerupclj.aggregation      → 집계 파이프라인                        (예정)
